{
  "name": "ramn-flasher",
  "version": "1.0.0",
  "private": true,
  "description": "Dev-only tooling for the RAMN Web Flasher. The app itself has no build step — open index.html.",
  "scripts": {
    "test": "node --test"
  }
}
